# Application upload docs (TEMPLATE)

Copy this to `README.md` locally if you want notes. Real PDFs/images stay git-ignored.

Place PDFs under `uploads/` (git-ignored):

| File | Use when |
|------|----------|
| `uploads/cs_degree.pdf` | Degree / diploma |
| `uploads/grade_report.pdf` | Transcript |
| `uploads/english_medium_instruction.pdf` | Only if the form asks |

Raw scans can live in `raw/` (also git-ignored).

Keep passport / ID out of git. If you need typed passport fields for forms, use a local `passport_data.md` (git-ignored).
